{
  "name": "svupa",
  "version": "0.0.1",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build:tailwind": "npx postcss static/tail.css -o static/styles.css",
    "dev": "npm run build:tailwind && vite dev --host",
    "prod": "npm run build && ls && ls ./build && node build/index.js",
    "build": "npm run build:tailwind && vite build",
    "package": "svelte-kit package",
    "preview": "svelte-kit preview"
  },
  "dependencies": {
    "tailwind": "^4.0.0"
  },
  "devDependencies": {
    "@supabase/supabase-js": "2.10.0",
    "@sveltejs/adapter-vercel": "next",
    "@sveltejs/kit": "next",
    "svelte": "next",
    "svelte-highlight": "7.2.0",
    "svelte-preprocess": "^4.10.4",
    "tailwindcss": "^3.2.7",
    "uuid": "9.0.0",
    "vite": "^4.0.0"
  }
}
